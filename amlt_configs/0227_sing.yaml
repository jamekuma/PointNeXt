description: pointnext
target:
  service: sing
  name: msroctovc

environment:
  image: jamekuma/pointnext_cls:latest
  
storage:
  output:
    storage_account_name: igsharestorage
    container_name: v-jixiong
    mount_dir: /mnt/output
  input:
    storage_account_name: igsharestorage
    container_name: v-jixiong
    mount_dir: /mnt/input
code:
  local_dir: .


jobs:

# - name: 0222_pointnext
#   sku: G1
#   preemptible: False
#   command:
#   - bash my_scripts/0222_pointnext.sh

- name: 0222_pointnext_repsurf
  sku: G1
  preemptible: False
  command:
  - bash my_scripts/0222_pointnext_repsurf.sh